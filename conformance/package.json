{
  "name": "@dispatchitapp/conformance",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-language conformance runner: every SDK's produced events are normalized and diffed against the golden fixtures.",
  "type": "module",
  "scripts": {
    "test": "node --test"
  }
}
